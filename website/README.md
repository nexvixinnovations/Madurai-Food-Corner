# Customer Web Application

Built with React 18, Vite, TypeScript, React Router, and TailwindCSS.

## Development Setup

```bash
npm install
npm run dev
```

The app will launch at `http://localhost:5173`.
